
# This file was generated by the Tkinter Designer by Parth Jadhav
# https://github.com/ParthJadhav/Tkinter-Designer


from pathlib import Path

# from tkinter import *
# Explicit imports to satisfy Flake8
from tkinter import Tk, Canvas, Entry, Text, Button, PhotoImage

import subprocess
import ctypes

def minimize_window(): #Provided by StackOverflow
    # Get a handle to the current window
    hwnd = ctypes.windll.user32.GetForegroundWindow()
    
    # Minimize the window
    ctypes.windll.user32.ShowWindow(hwnd, 6)  # 6 corresponds to SW_MINIMIZE

def restore_window(hwnd):
    # Restore the window
    ctypes.windll.user32.ShowWindow(hwnd, 9)  # 9 corresponds to SW_RESTORE

def run_python_file(file_path):
    try:
        hwnd = ctypes.windll.user32.GetForegroundWindow()  # Get the handle of the current window

        minimize_window()  # Minimize the current window

        # Launch the new script in a new Python process
        process = subprocess.Popen(["python", file_path])

        # Wait for the new script to finish
        out, err = process.communicate()

        # Restore the previous window
        restore_window(hwnd)
    except subprocess.CalledProcessError as e:
        print(f"Error: {e}")
    except FileNotFoundError:
        print("Error: Python interpreter not found. Please make sure Python is installed.")

def ButtonRegistrationClick_Execution():
    sScriptPath = r'C:\Users\Kai\Desktop\Project\Screen 2 - Registration\build\gui.py'  
    run_python_file(sScriptPath)

OUTPUT_PATH = Path(__file__).parent
ASSETS_PATH = OUTPUT_PATH / Path(r"C:\Users\Kai\Desktop\Project\Screen 1 - Intro\build\assets\frame0")


def relative_to_assets(path: str) -> Path:
    return ASSETS_PATH / Path(path)


window = Tk()

# Get the screen width and height
screen_width = window.winfo_screenwidth()
screen_height = window.winfo_screenheight()

# Calculate the x and y coordinates to center the window
x = (screen_width - 640) // 2  # 640 is the window width
y = (screen_height - 320) // 2  # 320 is the window height

# Set the window geometry
window.geometry("640x320+{}+{}".format(x, y))

window.configure(bg = "#FFFFFF")


canvas = Canvas(
    window,
    bg = "#FFFFFF",
    height = 320,
    width = 640,
    bd = 0,
    highlightthickness = 0,
    relief = "ridge"
)

canvas.place(x = 0, y = 0)
image_image_1 = PhotoImage(
    file=relative_to_assets("image_1.png"))
image_1 = canvas.create_image(
    320.0,
    160.0,
    image=image_image_1
)

image_image_2 = PhotoImage(
    file=relative_to_assets("image_2.png"))
image_2 = canvas.create_image(
    89.0,
    163.0,
    image=image_image_2
)

image_image_3 = PhotoImage(
    file=relative_to_assets("image_3.png"))
image_3 = canvas.create_image(
    279.0,
    265.0,
    image=image_image_3
)

image_image_4 = PhotoImage(
    file=relative_to_assets("image_4.png"))
image_4 = canvas.create_image(
    278.0,
    195.0,
    image=image_image_4
)

image_image_5 = PhotoImage(
    file=relative_to_assets("image_5.png"))
image_5 = canvas.create_image(
    278.0,
    126.0,
    image=image_image_5
)

image_image_6 = PhotoImage(
    file=relative_to_assets("image_6.png"))
image_6 = canvas.create_image(
    388.0,
    59.0,
    image=image_image_6
)

button_image_1 = PhotoImage(
    file=relative_to_assets("button_1.png"))
button_1 = Button(
    image=button_image_1,
    borderwidth=0,
    highlightthickness=0,
    command=ButtonRegistrationClick_Execution,
    relief="flat"
)
button_1.place(
    x=425.0,
    y=171.0,
    width=182.0,
    height=37.0
)
window.resizable(False, False)
window.mainloop()
